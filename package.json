{
	"name": "onsi-electron",
	"productName": "ONSI",
	"version": "1.0.0",
	"description": "",
	"main": "main.js",
	"scripts": {
		"build": "gulp clean && gulp move",
		"watch": "npm run build -w",
		"start": "npm run build && electron ./main.js",
		"package-windows": "electron-packager . ONSI --platform=win32 --arch=x64 --overwrite --prune=true --out=builds --asar"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"electron": "^1.3.0",
		"electron-packager": "^12.2.0",
		"electron-rebuild": "^1.8.2",
		"gulp": "^3.9.1",
		"gulp-clean": "^0.4.0"
	},
	"dependencies": {
		"request": "^2.88.0"
	}
}
